"use strict";

// 'use strict';
$(function () {
  var data = {
    name: 'center'
  };
  console.log(data);
}); // 'use strict';

$(function () {
  var data = {
    name: 'index'
  };

  var foo = function foo() {
    data = Object.assign(data, {
      age: '18'
    });
  };

  foo();
  var keys = Object.keys(data);
  $('.box').html(keys);
});